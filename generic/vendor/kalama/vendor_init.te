# Copyright (c) Qualcomm Technologies, Inc. and/or its subsidiaries.
# SPDX-License-Identifier: BSD-3-Clause-Clear

domain_auto_trans(vendor_init, vendor_init_early_test_exec, vendor_init_early_test)

allow vendor_init rootfs:dir { read };
allow vendor_init self:qipcrtr_socket { read write };
allow vendor_init tmpfs:fifo_file create;
allow vendor_init device:file create;

allow vendor_init vendor_early_audio:process share;
allow vendor_init audio_device:chr_file create;
allow vendor_init audio_device:dir relabelfrom;
allow vendor_init default_in_lxc:dir mounton;
allow vendor_init device:file write;
allow vendor_init proc:filesystem mount;
allow vendor_init proc_asound:file getattr;
#allow vendor_init self:capability mknod;
allow vendor_init self:unix_dgram_socket ioctl;
allow vendor_init socket_device:sock_file write;
allow vendor_init tmpfs:file getattr;
allow vendor_init tmpfs:filesystem mount;
allow vendor_init device:chr_file ioctl;
allow vendor_init dmabuf_heap_device:dir relabelfrom;
allow vendor_init gpu_device:chr_file ioctl;
allow vendor_init graphics_device:chr_file ioctl;
allow vendor_init self:tcp_socket { create ioctl };
allow vendor_init socket_device:sock_file { create unlink };
allow vendor_init vendor_dmabuf_system_heap_device:chr_file relabelfrom;
allow vendor_init video_device:chr_file relabelto;
allow vendor_init rootfs:dir { add_name write };
allow vendor_init self:capability setgid;
allow vendor_init tmpfs:chr_file create;
allow vendor_init tmpfs:dir { create relabelfrom };
allow vendor_init audio_device:chr_file { relabelfrom relabelto };
allow vendor_init audio_device:dir relabelto;
allow vendor_init cgroup_v2:file getattr;
allow vendor_init cgroup_v2:filesystem mount;
allow vendor_init device:chr_file { create relabelfrom };
allow vendor_init device:dir { relabelfrom relabelto };
allow vendor_init devpts:chr_file { getattr open read setattr write };
allow vendor_init dmabuf_heap_device:chr_file { create relabelfrom };
allow vendor_init dmabuf_heap_device:dir relabelto;
allow vendor_init dmabuf_system_heap_device:chr_file relabelto;
allow vendor_init firmware_file:dir mounton;
allow vendor_init firmware_file:filesystem { mount relabelfrom relabelto };
allow vendor_init gpu_device:chr_file { map open read relabelfrom relabelto write };
allow vendor_init graphics_device:chr_file { map open read relabelfrom relabelto write };
allow vendor_init kernel:fd use;
allow vendor_init kernel:file { getattr read };
allow vendor_init null_device:chr_file relabelto;
allow vendor_init proc:filesystem { remount unmount };
allow vendor_init proc_bootconfig:file getattr;
allow vendor_init proc_cmdline:file getattr;
allow vendor_init proc_net:dir mounton;
allow vendor_init proc_random:file mounton;
allow vendor_init proc_sysrq:file mounton;
allow vendor_init random_device:chr_file relabelto;
allow vendor_init rootfs:dir mounton;
allow vendor_init rootfs:file { getattr open read };
allow vendor_init rootfs:filesystem unmount;
allow vendor_init rootfs:system module_load;
allow vendor_init self:capability { setpcap setuid sys_chroot };
allow vendor_init self:netlink_route_socket { bind create getattr nlmsg_readpriv nlmsg_write read setopt write };
allow vendor_init self:netlink_selinux_socket { read write create getattr setattr lock relabelfrom relabelto append bind connect listen accept getopt setopt shutdown recvfrom sendto name_bind };
allow vendor_init self:process setexec;
allow vendor_init self:qipcrtr_socket { create getattr };
allow vendor_init self:tcp_socket ioctl;
allow vendor_init self:udp_socket { create ioctl };
allow vendor_init socket_device:dir relabelto;
allow vendor_init socket_device:file { create relabelto };
allow vendor_init sysfs:dir mounton;
allow vendor_init sysfs:filesystem { mount unmount };
allow vendor_init sysfs_net:dir mounton;
allow vendor_init tmpfs:chr_file { getattr ioctl open read relabelfrom setattr write };
allow vendor_init tmpfs:dir { add_name mounton write };
allow vendor_init tmpfs:file { create mounton relabelfrom write };
allow vendor_init tmpfs:filesystem { remount unmount };
allow vendor_init tmpfs:lnk_file { create read };
allow vendor_init vendor_agm_device:chr_file relabelto;
allow vendor_init vendor_dmabuf_audio_ml_heap_device:chr_file relabelto;
allow vendor_init vendor_dmabuf_system_heap_device:chr_file { ioctl open read relabelto };
#allow vendor_init vendor_earlyVideo_exec:file execute_no_trans;
allow vendor_init vendor_early_audio:dir search;
allow vendor_init vendor_early_audio:file { open read };
#allow vendor_init vendor_early_services:file { append execute execute_no_trans lock };
#allow vendor_init vendor_init_exec:file { entrypoint execute_no_trans };
#allow vendor_init vendor_pd_mapper_exec:file execute_no_trans;
allow vendor_init vfat:filesystem relabelfrom;
allow vendor_init video_device:chr_file { ioctl open read relabelfrom write };
allow vendor_init app_in_lxc_exec:file execute_no_trans;
allow vendor_init audio_prop:property_service set;
allow vendor_init binderfs:filesystem mount;
#allow vendor_init bluetooth_prop:property_service set;
allow vendor_init cgroup_v2:file { ioctl watch };
allow vendor_init configfs:filesystem mount;
#allow vendor_init debugfs:filesystem mount;
allow vendor_init default_in_lxc:file { execute_no_trans mounton };
#allow vendor_init default_prop:property_service set;
allow vendor_init devpts:chr_file { ioctl watch watch_reads };
allow vendor_init devpts:filesystem mount;
allow vendor_init fusectlfs:filesystem mount;
allow vendor_init kernel:key link;
allow vendor_init kernel:security compute_create;
allow vendor_init mqueue:filesystem mount;
allow vendor_init node:tcp_socket node_bind;
allow vendor_init port:tcp_socket name_bind;
allow vendor_init proc:dir mounton;
allow vendor_init proc:file { getattr ioctl };
allow vendor_init proc_meminfo:file { getattr ioctl };
allow vendor_init proc_panic:file { getattr write };
allow vendor_init proc_pid_max:file { getattr write };
allow vendor_init proc_sysrq:file { getattr write };
#allow vendor_init self:binder set_context_mgr;
#allow vendor_init self:bpf { prog_load prog_run };
allow vendor_init self:capability { audit_write mknod net_bind_service sys_boot sys_resource };
# allow vendor_init self:capability2 { audit_read bpf checkpoint_restore perfmon };
allow vendor_init self:file ioctl;
allow vendor_init self:key { search setattr write };
allow vendor_init self:netlink_audit_socket { bind create getattr getopt nlmsg_relay nlmsg_write read setopt write };
allow vendor_init self:netlink_route_socket getopt;
allow vendor_init self:process setsockcreate;
allow vendor_init self:tcp_socket { bind ioctl listen read setopt write };
allow vendor_init self:udp_socket ioctl;
allow vendor_init selinuxfs:file write;
allow vendor_init tmpfs:blk_file create;
allow vendor_init tmpfs:dir { remove_name rmdir watch };
allow vendor_init tmpfs:file { append ioctl lock rename unlink watch };
allow vendor_init tmpfs:lnk_file { getattr rename unlink };
allow vendor_init tmpfs:sock_file { create getattr setattr unlink write };
#allow vendor_init usermodehelper:file { getattr write };
allow vendor_init vendor_earlyVideo_exec:file execute_no_trans;
allow vendor_init vendor_early_services:file { append execute_no_trans lock };
#allow vendor_init vendor_init_exec:file { entrypoint execute_no_trans };
allow vendor_init vendor_pd_mapper_exec:file execute_no_trans;
allow vendor_init vndbinder_device:chr_file getattr;
allow vendor_init weston:dir search;
allow vendor_init weston:file { getattr ioctl open read };
allow vendor_init weston:lnk_file read;

allow vendor_init devpts:chr_file ioctl;
allow vendor_init self:capability kill;
#allow vendor_init self:binder set_context_mgr;
#allow vendor_init self:bpf { prog_load prog_run };
#allow vendor_init self:capability sys_ptrace;
#allow vendor_init self:capability2 { audit_read bpf checkpoint_restore perfmon };
allow vendor_init self:file ioctl;
#allow vendor_init usermodehelper:file { getattr write };
#allow vendor_init vendor_early_services:file execute;
#allow vendor_init vendor_init_exec:file { entrypoint execute_no_trans };
allow vendor_init weston:file ioctl;
allowxperm vendor_init self:tcp_socket ioctl { SIOCBRADDBR SIOCBRADDIF };
allowxperm vendor_init self:udp_socket ioctl { SIOCSIFHWADDR SIOCGIFHWADDR };
allowxperm vendor_init self:unix_dgram_socket ioctl { SIOCGIFINDEX SIOCGIFNAME };
allowxperm vendor_init self:file ioctl { TCGETS };
allowxperm vendor_init devpts:chr_file ioctl { TCGETS TIOCVHANGUP KDSKBMODE TIOCNXCL KDSETMODE KDGKBMODE TIOCGLCKTRMIOS TIOCSLCKTRMIOS };
allow vendor_init init:dir search;
allow vendor_init init:file { open read };
allow vendor_init dmabuf_system_heap_device:chr_file relabelfrom;
allow vendor_init vendor_agm_device:chr_file relabelfrom;
allow vendor_init vendor_dmabuf_audio_ml_heap_device:chr_file relabelfrom;

allow vendor_init su:dbus send_msg;
allow su vendor_init:dbus send_msg;
